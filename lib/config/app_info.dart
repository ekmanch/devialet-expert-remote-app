/// Static app facts shown in Settings → About. [kAppVersion] must equal
/// `pubspec.yaml`'s `version` (a test reads the file and checks); no
/// runtime plugin is needed for a value that only changes on release.
const String kAppVersion = '1.0.0';

const String kGitHubUrl = 'https://github.com/ekmanch/devialet-expert-remote-app';
