# --enable-native-access: Gradle 9.3 on JDK 24+ (Android Studio JBR 25) otherwise
# prints JEP 472 "restricted method" warnings from its native-platform
# library on every build. Harmless, but noisy in the Run window.
org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError --enable-native-access=ALL-UNNAMED
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false
